# The DOS build journal — design notes, plan records, research arcs

The *why* behind each decision and the phased plans that shipped (or are still
on the bench). Nothing here is required to *use* DOS — the guides live in the
**[docs index](README.md)** — but this is the best way to understand the
kernel, and the right thing to read before changing it.

The docs are numbered in the order they were written (66 → 271); the number is
chronology, **not** a reading order. (A handful of early numbers —
82/84/99/117/139/156 — were written twice; the later-renumbered halves now live
at **182–187**, listed in the research-arc tables below. Three collisions are
still open, so a bare "docs/NN" reference to any of these is ambiguous until
one of each pair is renumbered: **191** —
`191_proactive-dos-banking-the-verdict-before-the-failure` vs
`191_tool-call-division-and-the-pretool-hook`; **125** — the
`125_go-hook-fastpath-build-plan` (the Go native fast-path, indexed below) vs the
`125_the-trifecta-color-and-the-capability-conjunction` design note; and **267** —
`267_durable-release-concepts-from-the-userland-app` vs
`267_the-webhook-notifier-the-universal-transport-on-the-notify-seam`. Each pair's
two halves are distinct documents that happen to share a chronology number; cite
them by full filename, not the bare number.)

## Design notes — the *why* (read for understanding)

These are essays, not plans. They explain the thinking the code rests on; none of
them is required to *use* DOS, but they're the best way to understand it.

| Doc | The idea |
|---|---|
| [79_primitives-not-features](79_primitives-not-features.md) | Why the syscalls are deliberately small — a primitive you build *on*, not a feature you consume. The restraint that makes a substrate. |
| [182_the-kernel-is-a-taxonomy-of-refusal](182_the-kernel-is-a-taxonomy-of-refusal.md) | All four syscalls are kinds of "no" — disbelief, decline, deny-effect, deny-anonymity. Refusal is the load-bearing primitive. |
| [102_when-to-trust-an-agent](102_when-to-trust-an-agent.md) | The other half of the slogan: *when is it reasonable to trust an agent?* Trust structure not content (the Linux `copy_from_user` posture), prior commitments not reports (a plan is a predicate — it turns undecidable "good work?" into decidable "conforms?"), and only where a wrong yes is detectable + reversible (which *derives* the ORACLE→JUDGE→HUMAN layering). Frames each core problem against its next-best alternative. |
| [103_memory-is-an-unverified-agent](103_memory-is-an-unverified-agent.md) | Applies 102's trust law to the agent's *own persistent memory*: a memory file is a frozen self-report, and recall injects it as fact. Reproduced live on this repo's store (a "FIXED cli.py" claim that git says shipped two commits ago). Every memory failure is a syscall re-aimed — stale "FIXED" is `verify`, aging facts is `liveness`, confidently-wrong recall is the missing `refuse`/abstain rung, index bloat is `arbitrate` over topics. The fix is a recall *driver* (a consumer, not a kernel change): re-verify a memory's checkable claims against ground truth at read, surface a closed `RECALL_FRESH/DRIFTING/STALE/UNVERIFIABLE` verdict, never the bare claim. |
| [108_the-cheap-lie-and-the-narration-taxonomy](108_the-cheap-lie-and-the-narration-taxonomy.md) | The realistic fleet failure isn't the *fabricated diff* (rare, expensive, shape-checkable) — it's the cheap, confident one-liner *"all work completed"* a worker emits over an empty tree: common, costless, eye-undetectable, and the line `verify()` is built to ignore. Lays out the four-way narration taxonomy — fake-edit and the cheap fake-narration both bottom out on `verify` reading git (the flake is indistinguishable from the lie *without* it); intent-mismatch and context-collapse escape the oracle to the JUDGE/HUMAN rung. Follows the regress down: the judge is itself a narrator (contained, advisory-only, not made correct), and it terminates only at the non-narrating, forgery-proof git floor. |
| [76_flexible-goals-and-verification](76_flexible-goals-and-verification.md) | Where the system *bends* (which signals, which provenance) and where it stays *rigid* (the adjudication itself). |
| [80_mcp-server-surface](80_mcp-server-surface.md) | DOS as a tool any agent can call — the MCP server as the lowest-friction adoption door. |
| [81_velocity-economics-and-the-fleet-benchmark](81_velocity-economics-and-the-fleet-benchmark.md) | The second axis of fleet effectiveness: catching the lie is integrity, *not paying for it downstream* is velocity. Defines the benchmark category. |
| [98_the-orchestrator-is-a-driver](98_the-orchestrator-is-a-driver.md) | The orchestrator (DOS-dispatch vs an external harness/ultracode `Workflow`) is just another driver of the same trust seam — comparable on one workload. Ships `dos lease-lane` (durable lease write-back over the pure arbiter) so a cross-process flow keeps collision-prevention. |
| [99_runtime-validation-and-the-actuation-boundary](99_runtime-validation-and-the-actuation-boundary.md) | The three moments of a run (before / *during* / after); the historical runtime failures are "alive-but-not-moving", already detected by `liveness()` but consumed by nothing. Closes the loop (`StopReason.SPINNING` self-stop) and draws the actuation line: the kernel may not kill because it must not know *what a process is* — self-stop is control flow (in-bounds); a foreign stop is *record + propose* only (the `reap`-family `halt` verb). |
| [84_ground-truth-trajectories-for-training](84_ground-truth-trajectories-for-training.md) | The kernel as a training-data instrument — every step emits a verified (claim, evidence, verdict) tuple. |

## Plan records — the *what shipped* (the build journal)

Each is a phased plan with a **Status** line at the top (✅ shipped / 🚧 partial /
committed-not-started). They document a specific capability and the decisions made
building it. Skim the Status line to see what's live.

| Doc | Capability | Status |
|---|---|---|
| [70_stamp-convention-plan](70_stamp-convention-plan.md) | The ship-stamp grammar as per-workspace data (`[stamp]`) — what `verify`'s grep rung recognizes | ✅ shipped |
| [71_workspace-config-readback-plan](71_workspace-config-readback-plan.md) | Reading `[lanes]`/`[paths]` back from `dos.toml` | ✅ shipped |
| [72_renderer-seam-plan](72_renderer-seam-plan.md) | The `--output` renderer seam (`dos.renderers` entry-point group) | ✅ shipped |
| [73_admission-predicate-plan](73_admission-predicate-plan.md) | Pluggable admission predicates (`dos.predicates`) + the `self-modify` guard | ✅ shipped |
| [74_skill-pack-plan](74_skill-pack-plan.md) | The generic skill pack shipped in the wheel + `dos doctor --json` / `dos gate` | ✅ shipped |
| [75_state-home-plan](75_state-home-plan.md) | Where DOS keeps state: per-project `.dos/` + machine-local `~/.dos` | ✅ shipped |
| [82_liveness-oracle-plan](82_liveness-oracle-plan.md) | `liveness()` — the temporal verdict (ADVANCING/SPINNING/STALLED) | 🚧 Phase 1 shipped |
| [184_the-supervisor-loop-plan](184_the-supervisor-loop-plan.md) | `supervise()` + `dos loop` — the fleet's init/PID-1: keep N dispatch-loops alive (spawn free lanes, reap STALLED, flag SPINNING) | 🚧 Phases 1–2 shipped |
| [78_typed-outcome-adoption-plan](78_typed-outcome-adoption-plan.md) | A minimal on-ramp: typed outcomes (reasons) as a standalone layer | committed, not started |
| [109_non-git-evidence-in-the-verify-verdict](109_non-git-evidence-in-the-verify-verdict.md) | A non-git rung ABOVE the git ladder in `verify()`'s verdict (build-green via the `ci_status` driver, then proc-liveness / audit-log / approval oracles) — conjunctive, gathered at the boundary, the driver discovered by name | 📋 planned |
| [110_the-concurrency-class-operator-surface](110_the-concurrency-class-operator-surface.md) | Make the already-landed arbiter `class_budgets` reachable: a `--class-budget` flag, a `[[concurrency_class]]` dos.toml table, and lifting the host soft-claim into the class registry (the docs/97 operator slice) | 📋 planned |
| [111_plan-scaffolding-and-automatic-plan-vs-oracle-checking](111_plan-scaffolding-and-automatic-plan-vs-oracle-checking.md) | A `dos`-scaffolded starter plan (+ a doctor harvest-check), and the watchdog driver auto-fanning the oracle over the active plan so an over-claiming loop routes into `dos decisions` | 📋 planned |
| [112_the-dynamic-verified-by-dos-badge](112_the-dynamic-verified-by-dos-badge.md) | The live verdict badge: a `dos badge` shields-endpoint JSON mode (3-state honesty — via-none = neutral, never false-accuse), then the CI Action, then an optional hosted endpoint | 📋 planned |
| [120_the-status-digest-a-folded-fact-for-a-fleet](120_the-status-digest-a-folded-fact-for-a-fleet.md) | `dos status <run_id>` — the phased build of [116](116_the-durable-commons-and-the-constrained-a2a-problem.md) §5: folds the four shipped reads (liveness / ledger-*verified* / held lease / resume verdict) into one fail-closed A2A fact (`claimed` absent by construction). Pure digest → CLI verb → MCP `dos_status` tool. The strategy §4 "hand-run CLI → platform-team infra" threshold | 🚧 Phase 1 shipped (`status.py`, 8 tests); Phases 2–4 planned |
| [207_dispatch-workflow-extraction-and-the-pickable-substrate-completion](207_dispatch-workflow-extraction-and-the-pickable-substrate-completion.md) | The second workflow-extraction plan after [74](74_skill-pack-plan.md): completes the [168](168_picker-substrate-and-quiet-completion-the-missing-syscalls.md) picker substrate (`enumerate` the phase-list producer + `reconcile` the quiet-completion gate — the two unbuilt of three; `pickable` shipped `8357ac0`), generalizes the host's per-pick cooldown into a kernel anti-churn primitive (an `ATTEMPT` WAL event + a `loop_decide` `PICK_COOLDOWN` rung), and ships the operator-facing skill tier (`dos-class-cycle` / `dos-promote` / `dos-unstick`) + a `dos init --skills` scaffold. Eight phases; three offline-replay gates (`enumerate` byte-parity over `job`'s 63 plans, cooldown re-pick backtest, `reconcile` on the toolathlon corpus). Includes a §"how well recent fixes reach DOS" back-flow audit | 📋 planned |
| [207-backflow-ledger](207-backflow-ledger.md) | The **live** job→DOS back-flow ledger (companion to 207): which `job` dispatch fixes reached the kernel (18 LANDED, auto-derived from in-code provenance citations) vs. which are STRANDED with a decided disposition (2 owed-a-lift → Phase 2/3, 3 scope-out). Regenerated by `scripts/backflow_ledger.py` (`--check` gates the docs/127 audit), pinned by `tests/test_backflow_ledger.py` | ✅ built / live |
| [66_dispatch-os-issue-solve-plan](66_dispatch-os-issue-solve-plan.md) | The foreign-repo "solve" path | research |

## Research arcs (144 →) — the recent build journal

The docs after ~143 cluster into a few parallel research lines rather than a single
chronology. Grouped by arc; within each, ordered by number. (The
[ENTERPRISEOPS_ARC.md](ENTERPRISEOPS_ARC.md) map is the deep guide to the
intervention/detector lines; this table is the index.)

### Intervention ladder & loop economics (integrity-slice defenses)

| Doc | The idea |
|---|---|
| [144_the-intervention-ladder-and-its-eval](144_the-intervention-ladder-and-its-eval.md) | Confidence-gated OBSERVE / WARN / DEFER / BLOCK ladder over distrust verdicts; measured on EnterpriseOps, WARN is the optimum when the mattered-rate is low, BLOCK only wins above ~0.80. |
| [145_the-loop-economics-axis-and-the-stall-reader](145_the-loop-economics-axis-and-the-stall-reader.md) | The task-completion slice: a `tool_stream` stall reader (ADVANCING/REPEATING/STALLED) that survives strong models — prevents loops without vanishing as capability rises. |
| [151_intervention-ladder-live-study](151_intervention-ladder-live-study.md) | Methods + results from the live docs/144 experiment on the ServiceNow benchmark: WARN is the optimum, a non-disruptive BLOCK is neutral-at-best. |
| [152_the-first-natural-lift-experiment-dangling-intent-conversion](152_the-first-natural-lift-experiment-dangling-intent-conversion.md) | Dangling-intent WARN detector live A/B (100 natural tasks, 0 mint-rate): DETECT works (9% fire, 0% false), FIX inert on a strong model — awaits a weaker baseline. |

### EnterpriseOps-Gym arc (coordination & the measured failure distribution)

| Doc | The idea |
|---|---|
| [146_the-slice-survey-which-verifier-class-is-worth-a-plan](146_the-slice-survey-which-verifier-class-is-worth-a-plan.md) | An adversarial 3-analyst survey grading Integrity / Task-Completion / Policy-Compliance / Refusal slices; names the refusal subset (precursor gate) as the next plan. |
| [147_the-precursor-presence-gate](147_the-precursor-presence-gate.md) | A byte-clean refusal detector: "did the model MINT this id or RESOLVE it?" — kernel BUILT + GREEN; consumer wiring + hand-authored grammar remain. |
| [148_running-tests-and-sims-concurrently](148_running-tests-and-sims-concurrently.md) | A map of safe parallelism levels for this repo's tests/sims, grounded in `pytest -n auto` + WAL/config isolation knobs. |
| [149_the-real-failure-distribution-sorts-the-priorities](149_the-real-failure-distribution-sorts-the-priorities.md) | Measured corpus analysis: 92% of premature stops are "silent" (undetectable) — until docs/150 cracks the claim and recovers the narrating stopper as byte-clean. |
| [150_the-dangling-intent-detector-cracks-149](150_the-dangling-intent-detector-cracks-149.md) | The steelman of docs/149: a dangling-intent detector catches narrating premature stops (admission against interest) — 13% recall / 0% false on the real corpus. |
| [153_can-dos-lift-a-weak-model](153_can-dos-lift-a-weak-model.md) | An honest proof point: DOS recovers the execution-substrate fraction of weak-model failures (minted ids, loops, narrating stops) — low single-digit pp, not a gap-closer. |
| [219_the-capability-routing-floor-why-model-agnostic-is-the-point](219_the-capability-routing-floor-why-model-agnostic-is-the-point.md) | The *complement* to 153 (route, don't lift): DOS's zero-model-token verdicts make "capable enough" capability-routing **safe** — the floor never sags when the model gets cheaper because it never stood on the model. Live proof: the reference app runs the most expensive model on an orchestrator whose every hard decision is already a $0 verdict it is forbidden to re-derive. |
| [155_coordinating-parallel-research-on-dos](155_coordinating-parallel-research-on-dos.md) | Resolving a parallel-agent collision by splitting numbered docs into identifier / immutability / finding; uses measured rows, not claims, as the coordination primitive. |

### Detector line (byte-clean signal on frozen trajectories)

| Doc | The idea |
|---|---|
| [157_toolathlon-replay-detector-purchase](157_toolathlon-replay-detector-purchase.md) | A $0 replay over the Toolathlon-Trajectories dataset (22 models × 3 runs × 108 tasks, third-party scored): the DETECT-not-FIX strategy, measuring in-flight byte-clean detectors on an open benchmark. |
| [158_recall-expansion-silent-and-frontier-failures](158_recall-expansion-silent-and-frontier-failures.md) | Ships the `terminal_error` detector: union recall 4.74% → 6.18% (92.6% precision), +12 net-new on the frontier (o3, claude-4.5-sonnet); first detector additive on the reasoning frontier. |
| [159_naive-baselines-and-what-a-detector-default-should-be](159_naive-baselines-and-what-a-detector-default-should-be.md) | Naive variants + a head-to-head refute "naive did better"; reveals recall is the WRONG scoreboard for this benchmark and extracts the defaults the next detector should start from. |
| [160_sota-positioning-the-trained-classifier-and-the-arbiter-neighbors](160_sota-positioning-the-trained-classifier-and-the-arbiter-neighbors.md) | The trained-classifier baseline (the "98%" paper) does NOT beat a zero-training detector on the scoreboard that matters; arbiter neighbors solve a different axis; the forward path is a fresh third-party witness. |
| [161_toolathlon-live-gemini-run](161_toolathlon-live-gemini-run.md) | Closes the docs/157 gap: stand up the real Toolathlon env, drive gemini-2.5-flash × 27 tasks, score with the third-party oracle, re-fold detectors on our own runs; reproduces the frozen-study numbers. |
| [162_the-recovery-knob-and-the-false-reassurance-failure](162_the-recovery-knob-and-the-false-reassurance-failure.md) | A surgical recovery knob (`specific-only`): +70 net-new catches at 92% precision by distinguishing "same tool" from "same operation" on general-purpose executors. |
| [163_doing-good-with-a-terminal-error-catch-the-warn-gate](163_doing-good-with-a-terminal-error-catch-the-warn-gate.md) | The byte-clean terminal_error → WARN gate (offline counterfactual: 93% of 76 catches hit clearable walls); shipped this session, a live A/B the bounded next step. |
| [173_dos-for-tool-problems-the-audit-grounded-detector-line](173_dos-for-tool-problems-the-audit-grounded-detector-line.md) | A tool call is exactly where an agent narrates an effect on shared state it did not author; the byte-author invariant keys all tool-failure verdicts on environment-written bytes, not agent-written. |
| [174_byte-clean-detector-on-popular-benchmarks-the-replay-target](174_byte-clean-detector-on-popular-benchmarks-the-replay-target.md) | Can the structural error-channel + unrecovered-error gate (83.8% precision on AgentHallu) apply to more popular benchmarks for a harder landing? A build-vs-extend decision memo. |
| [176_live-trajectory-verification-walk-rewalk-prune](176_live-trajectory-verification-walk-rewalk-prune.md) | Live trajectory verification: the walk / re-walk / prune cycle over an in-flight run. |
| [177_the-frontier-silent-failure-and-why-detection-is-not-solved](177_the-frontier-silent-failure-and-why-detection-is-not-solved.md) | The frontier silent-failure problem — why detection is not a solved question even on strong models. |
| [179_detector-self-labeling-harness](179_detector-self-labeling-harness.md) | More data → more signal: join each detector FIRING (env-authored) to the run's git-minted outcome to mint a `(signal, was-it-real)` calibration label. The one fold that mints NEW ground truth; the design law that distinguishes it from re-counting aggregators (tree-verify / per-turn ratio / fleet-roll). `dos.firing_label` + `dos.fleet_roll` SHIPPED. |
| [188_frontier-loop-rate-the-kill-criterion-measured](188_frontier-loop-rate-the-kill-criterion-measured.md) | The frontier loop-rate, *measured*: across 1,795 real Opus-4.x Claude Code sessions / 40,820 tool calls, the live `tool_stream` verdict's fire-rate is the rank-1 kill-criterion — and it fired (the WARN-frontier bet died at 0.2% substantive). |
| [192_the-world-state-witness-ladder-and-the-w2-w3-gap](192_the-world-state-witness-ladder-and-the-w2-w3-gap.md) | Corrects docs/177: "world state" is a 4-rung ladder (W0–W3); only W3 (gold-diff) witnesses the GOAL. DOS's `verify()` file-path rung is **W2-presence, not W3** — a plan-authored projection, code-confirmed but never content-diffed; ~38% of frontier goals reach no sound witness. |

### Evaluation positioning & benchmark alignment

| Doc | The idea |
|---|---|
| [154_faster-closed-loop-verification](154_faster-closed-loop-verification.md) | The loop-bandwidth axis: in-flight vs post-hoc, incremental vs batch, push vs pull, parallel vs serial; the purity discipline (pure `classify` + an I/O boundary) is what makes the verdicts testable. |
| [156_grounded-rag-adoption-and-the-claim-ledger-seam](156_grounded-rag-adoption-and-the-claim-ledger-seam.md) | Third-party grounded-RAG adoption: the hard guarantee comes from a re-execution harness (recompute the cited SQL), not DOS; DOS's structural contribution is one rule — an AGENT_AUTHORED judge never auto-passes. |
| [166_emerging-fleet-governance-benchmarks-and-the-specbench-probe](166_emerging-fleet-governance-benchmarks-and-the-specbench-probe.md) | A landscape scan of 2026 governance benchmarks (most measure the trust gap; few ship a deterministic adjudicator); the SpecBench probe-first finds no trajectory dataset available. |
| [167_the-eval-and-naive-verifier-comparison](167_the-eval-and-naive-verifier-comparison.md) | The taxonomy: evals score actors / naive verifiers read the bytes the actor wrote / DOS reads the bytes the actor COULD NOT — one axis (`Accountability`) sorts all three. |
| [168_picker-substrate-and-quiet-completion-the-missing-syscalls](168_picker-substrate-and-quiet-completion-the-missing-syscalls.md) | A design concept: recurring wedges in the job consumer + the trajectory-audit suggest missing syscalls for picker-substrate & quiet-completion — a companion to `picker_oracle` + the completion verdict. |
| [169_where-dos-verification-helps-eval](169_where-dos-verification-helps-eval.md) | A survey: every popular agent benchmark grades against a forgeable proxy; DOS productizes the hand-audit every team already ran, converting a binary score into a typed-evidence corpus. |
| [187_benchmark-telemetry-as-self-report](187_benchmark-telemetry-as-self-report.md) | Benchmark cache-token telemetry is system-authored evidence; maps the provenance chain to the DOS accountability ladder, catalogs the insertion surfaces, ranks the work. *(Renumbered from 156.)* |

### Frontier lift & long-horizon value

| Doc | The idea |
|---|---|
| [170_frontier-lift-where-it-lives-and-the-features-that-grow-it](170_frontier-lift-where-it-lives-and-the-features-that-grow-it.md) | Frontier value lives on the fleet-N × horizon × fanout axis, vanishes at N=1, is amplified by throughput not by fixing the model; a design roadmap of the features that grow it. |

### FIX loop & the rewind verdict

| Doc | The idea |
|---|---|
| [164_the-fix-loop-and-the-rewind-verdict](164_the-fix-loop-and-the-rewind-verdict.md) | A principled path across the DETECT-not-FIX boundary without the kernel becoming a contestant; F0/F1 are mostly assembly over existing primitives, the F3 prize needs the docs/126 PEP. |
| [172_the-rewindable-fix-loop-experiment](172_the-rewindable-fix-loop-experiment.md) | The rewindable FIX loop wired end-to-end with a cheap experiment on EnterpriseOps-Gym — a falsifiable experiment a frontier lab would care about. |
| [175_rewind-on-toolathlon-the-disjoint-failure-class](175_rewind-on-toolathlon-the-disjoint-failure-class.md) | Applying the rewind FIX idea from EnterpriseOps (injected mints) to Toolathlon (naturally-occurring failures) on a third-party-scored benchmark — independent corroboration on a different corpus. |
| [190_coordination-measured-and-the-f3-gateability-split](190_coordination-measured-and-the-f3-gateability-split.md) | Coordination, *measured*: ≥5 concurrent same-file collisions at 10s (3.3s tightest) that do **not** decay with capability — and the F3 underfill split (gateable via env-error bytes vs omission → human). |
| [193_clean-restart-seeded-with-dos-knowledge](193_clean-restart-seeded-with-dos-knowledge.md) | The missing comparand to rewind: a *clean restart* seeded with DOS knowledge drops the polluted prefix and structurally escapes the rewind livelock; the $0 gate says PROCEED, the live smoke fires. |
| [194_the-livelock-and-why-the-dominant-natural-thrash-is-uncurable-in-loop](194_the-livelock-and-why-the-dominant-natural-thrash-is-uncurable-in-loop.md) | The two livelocks are distinct: the mint-injection one (rewind re-invents the omitted id) and the *natural* one (the agent re-issues a wrong call). The dominant natural thrash is uncurable in-loop. |
| [198_the-feasibility-witness-and-give-up-correctly](198_the-feasibility-witness-and-give-up-correctly.md) | The livelock line asked the wrong question. Split walled-vs-curable with a byte-clean **feasibility witness** first (the dominant `create_filter` thrash is INFEASIBLE, 0/579 — not curable); "give-up-correctly" (early-halt) passes on a clean denominator. Supersedes the natural-thrash framing. |
| [199_the-curable-slice-conversion-experiment-the-data-never-collected](199_the-curable-slice-conversion-experiment-the-data-never-collected.md) | The one fresh-data spend docs/198 left open: the curable-slice conversion A/B — what to collect, why, and the answer being "a sharper denominator," not "more of the same." |

### KV-cache (adjudicate the cache, never manage it)

| Doc | The idea |
|---|---|
| [195_dos-and-the-kv-cache-the-forward-direction](195_dos-and-the-kv-cache-the-forward-direction.md) | The forward question (sibling of docs/178's backward one): should DOS manage, route, or *adjudicate* the KV cache? It adjudicates — tail-truncate is the only sound+cheap prefix cut; DOS never manages the cache. |
| [196_dos-cache-integration-the-witness-and-the-verified-prefix](196_dos-cache-integration-the-witness-and-the-verified-prefix.md) | The build plan: a `cache_witness` EvidenceSource + a verified-prefix HINT, kernel untouched; Mooncake / LMCache / Dynamo as consumers. |

### Runtime binding & skills (Claude Code)

| Doc | The idea |
|---|---|
| [165_dos-into-claude-code-the-runtime-binding-roadmap](165_dos-into-claude-code-the-runtime-binding-roadmap.md) | Bind the kernel to the moments an agent cannot fake: map every Claude Code extension seam to a DOS verdict, rank the bindings, name the ~5-line fix that unblocks the Stop family. Design, verified against source. |
| [171_dos-in-skills-and-the-read-loop-proof](171_dos-in-skills-and-the-read-loop-proof.md) | The skill pack teaches the syscalls by worked example (six SKILL.md screenplays + an EXAMPLES.md cookbook); `tool_stream` solves the audited read-loop pathology (22 identical reads → pre-empt ~17). |
| [189_ideas-from-the-claude-code-source-extensible-to-dos](189_ideas-from-the-claude-code-source-extensible-to-dos.md) | A deep audit of the Claude Code v2.1.88 source (10-reader fan-out, 85 ideas, hand-verified) for mechanisms liftable into DOS: the headline is **CC ships the PEP DOS lacks** — an `EnforcementHandler` seam + `PreToolUse` deny + hook-outcome journal records are the ranked next build; a config-linter, memory-provenance spine, message-keyed file checkpoints, and a diminishing-returns loop gate follow. |
| [191_proactive-dos-banking-the-verdict-before-the-failure](191_proactive-dos-banking-the-verdict-before-the-failure.md) | The proactive lens: use the verdict *before* a failure, on a non-agent denominator, by occupying the host's own seams (PostToolUse, settings.json). Ranked plays + honest cuts; the cheapest move is one `settings.json` block. *(One of two docs numbered 191.)* |
| [191_tool-call-division-and-the-pretool-hook](191_tool-call-division-and-the-pretool-hook.md) | A tool call is three moments (PRE / BOUNDARY / STOP); evidence-locus rises and actuation falls, crossing at PRE — the only deny-capable *and* sound cell. Ships `dos hook pretool` (`permissionDecision: deny`). *(One of two docs numbered 191.)* |
| [197_how-dos-is-directly-useful-to-ultracode](197_how-dos-is-directly-useful-to-ultracode.md) | The third question docs/98 + docs/128 leave open: once an ultracode subagent returns, what in its return is trustworthy? Trajectory-grounded (2305 real subagents) — **32% return a harness-authored `<synthetic>` 429 rate-limit string that folds as a "finding"**; this very workflow exhibited it (4/6 agents died, reported `completed`). The fold site is the one unverified-self-report boundary; the lever table maps each gap to the byte-clean syscall (`effect_witness`/`verify`/`arbiter`/`tool_stream`) that reads a *different* byte-author, with the honest shipped/unwired/named-gap split. Build: `dos verify-result` + a witness-routing convention. |

### Native binding (the Go fast-path) & the cross-vendor seam

The hooks bind DOS to a host runtime, and there are two threads here: a **native Go
`dos-hook`** that erases the per-tool-call Python cold-start, and a **cross-vendor
dialect seam** that lets one decided verdict render into any host's hook grammar.
The decision logic is unchanged — both are about *delivery*: how fast the verdict
arrives, and in whose envelope.

| Doc | The idea | Status |
|---|---|---|
| [125_go-hook-fastpath-build-plan](125_go-hook-fastpath-build-plan.md) | **GHF** — the native Go `dos-hook` binary. The per-tool-call hook hot path pays ~0.3–0.8 s of Python interpreter cold-start on *every* call; a static no-cgo Go binary erases it. Adopts the docs/100 pure-decider boundary + the docs/124 byte-exact parity contract: Go is a PURE decider over the decision-bearing fields, prose stays matchable-or-Python. Serves `pretool`/`posttool`/`marker`/`stop`; `hooks.json` calls the bundled launcher `\|\| python` so a missing binary never blocks. *(One of two docs numbered 125 — see the ambiguity note above.)* | 🚧 GHF1–4 shipped; GHF5 (native `verify`/oracle port) open |
| [270_go-hook-fastpath-benchmarks](270_go-hook-fastpath-benchmarks.md) | **The witness for docs/125's speed claim.** docs/125 shipped on the *argument* that a static binary erases the cold-start; this is the *measurement*: **Go ~6–15 ms vs Python ~230–262 ms per hook call (16–43×), byte-identical output on every real spawn.** Attribution nails it — `import dos.cli` alone is ~153 ms, which the binary structurally has zero of. The first `testing.B` in the Go module + an end-to-end process-latency harness (`scripts/bench_hook_e2e.py`). | ✅ measured |
| [271_the-pure-go-frontier](271_the-pure-go-frontier.md) | **What's left to port to Go, and what must never be.** The forward plan after the benchmark: the dialect transcoder (the docs/268 gap, now closing), GHF5 (the native `verify`/oracle rung, blocked on exactly 3 RE2-incompatible lookbehinds in `phase_shipped`/`stamp`), cross-compile/on-device (docs/122), and the hard boundary — the MCP server and the heavy-I/O kernel stay Python by construction. | 📋 planned |
| [385_the-strongly-typed-mandate-and-the-python-retreat-plan](385_the-strongly-typed-mandate-and-the-python-retreat-plan.md) | **The operator direction (2026-06-17): strongly-typed by default; Python retreats to named seams.** Reverses docs/271 §5's "the kernel stays Python" and docs/100's "Python is the spec." The mechanism is unchanged — the same parity ratchet — but its truth-pointer **flips**: per decider, port → soak byte-green → make **Go** the spec, thin or delete the Python twin. Names the four seams Python keeps (the `import dos` binding over the Go core, MCP, the test harness, the OS calls) and the phased retreat (TP1 flip the shipped hook deciders → TP2 the RE2-clean cores → TP3 the oracle/`verify` recognizer → TP4 the I/O → TP5 the Python-as-binding → TP6 the typed CLI/TUI, last). The binding rule lives in [CLAUDE.md](../CLAUDE.md) "Strongly typed by default". | 📋 planned |
| [217_the-cross-vendor-hook-dialect-seam](217_the-cross-vendor-hook-dialect-seam.md) | The `HookDialect` renderer seam — `decide()` returns a dialect-neutral `HookVerdict`; a per-host renderer (`claude-code` default + `gemini`/`codex`/`cursor`) emits the right envelope, selected by `--dialect`. The third instance of the `judges`/`overlap_policy` pure-protocol + by-name-resolver pattern, on the *output* side. *(Also indexed in the companion-docs list below.)* | ✅ shipped (Python) |
| [221_the-cross-vendor-hook-installer](221_the-cross-vendor-hook-installer.md) | `dos init --hooks <host>` — write the right settings file for each host (CC `settings.json`, Gemini `.gemini/settings.json`, Cursor, Codex), wiring each event to `dos hook … --dialect <host>`. The install-side companion to the docs/217 render-side seam. | ✅ shipped |
| [268_gemini-integration-proof-and-the-go-dialect-gap](268_gemini-integration-proof-and-the-go-dialect-gap.md) | The live Gemini integration proof — and the finding that the **Go binary detects a refusal but emitted the Claude-Code envelope unconditionally**, a latent fail-*open* for any non-CC host wired to the fast path. The audit + design for the Go-side dialect transcoder (now being built — see docs/271). | 🚧 gap identified; fix in flight |
| [269_antigravity-the-fifth-host](269_antigravity-the-fifth-host.md) | Adding Google Antigravity as the fifth host — the clean confirmation that the docs/217 render seam + docs/221 installer generalize: a `drivers/hook_dialects.py` renderer + an install-spec + two entry-point rows, and **nothing under `src/dos/*.py` changes.** A vendor is a data row. | ✅ shipped |

### Renumbered design notes (182 → 187)

These were written under an already-used number; the more-referenced half kept the
original number and these moved here. Listed by topic — see the section each
belongs to above for context.

| Doc | The idea | Was |
|---|---|---|
| [182_the-kernel-is-a-taxonomy-of-refusal](182_the-kernel-is-a-taxonomy-of-refusal.md) | All four syscalls are kinds of "no" — disbelief, decline, deny-effect, deny-anonymity. Refusal is the load-bearing primitive. *(Also linked in Design notes above.)* | 82 |
| [183_how-much-does-this-lean-on-git](183_how-much-does-this-lean-on-git.md) | How much does `verify()` lean on git? — the floor of ground truth, and why a floor is still load-bearing. | 84 |
| [184_the-supervisor-loop-plan](184_the-supervisor-loop-plan.md) | `supervise()` + `dos loop` — the fleet's init/PID-1. *(Also in Plan records above.)* | 99 |
| [185_native-log-adapters-and-the-actor-witness-split](185_native-log-adapters-and-the-actor-witness-split.md) | Native log adapters — and why a log is only evidence when the actor isn't the witness. | 117 |
| [186_live-journal-and-decision-surface-audit-2026-06-04](186_live-journal-and-decision-surface-audit-2026-06-04.md) | A live audit of the lane journal + decision surface (2026-06-04). | 139 |
| [187_benchmark-telemetry-as-self-report](187_benchmark-telemetry-as-self-report.md) | Benchmark telemetry is self-report — the L3-token verification problem. *(Also in Evaluation positioning above.)* | 156 |

### The agent-kernel reframe — the tool call as a system call (2026-06-16)

A design arc dispatched from an operator brainstorm: if an agent's primary job is
tool-calling, the tool call is its *privilege boundary* — so adjudicate it like a
syscall, in-process, before it fires. Notes only (📋 planned); the strategy framing
lives in the `dos-private` repo (`dispatch-os-tool-call-is-a-syscall.md`,
`dispatch-os-the-fused-agent-kernel.md`).

| Doc | The idea |
|---|---|
| [372_the-tool-call-as-syscall-the-adjudicated-call-layer](372_the-tool-call-as-syscall-the-adjudicated-call-layer.md) | The altitude argument: promote the tool call to the agent OS's system call, and DOS's existing verdicts (`verify`/`arbitrate`/`refuse`) become the kernel's reference monitor on the call path — LSM/seccomp/eBPF for agents. Maps each borrowed OS primitive (syscall table → tool ABI, vDSO → cheap-call fast-path, MMU → context-as-protected-memory, error codes → the refusal vocabulary returned *before* the call) to a DOS piece. Names the honest limit (the model isn't hostile; the tool table isn't closed). Commits no code; motivates 373 + 374. |
| [373_preflight-mechanistic-verification-the-cheapest-rung-that-refutes](373_preflight-mechanistic-verification-the-cheapest-rung-that-refutes.md) | The buildable core: before a tool call executes, run the cheapest mechanistic check that could *refute* it (rung 0 static → rung 1 dry-run → rung 2 probe → rung 3 fire), short-circuit on a refute. The operator's `pytest`→`--collect-only` example generalized; a refuted call never polluted context. Proposes a pure `preflight()` core + `dos preflight` verb + a `dos.toml [preflight]` registry, fail-to-abstain (refuse-more-only). Generates its own RSI signal (every "passed rung k, failed k+1" tightens the ladder). |
| [374_ten-x-stewards-one-invariant-per-steward](374_ten-x-stewards-one-invariant-per-steward.md) | "10× more cron tasks like stewards": a *population* of small, single-invariant, fail-to-abstain stewards that both validate and steer, gardened by a meta-steward that prunes non-firing ones. No new kernel mechanism — reuses the supervisor seam ([210](210_the-supervise-config-seam.md)), the `dos-class-cycle` judge, and the self-tuning loop ([365](365_self-tuning-enforcement-policy-the-pep-feedback-loop.md)), run wider and cheaper. Ships a starter catalog as `dos.toml [stewards]` rows + a `dos steward` runner. |

### Companion / process docs

- [next-stage-plan](next-stage-plan.md) — the directional foundation and index the committed plans above spawned.
- [74-seam-ledger](74-seam-ledger.md) / [74-friction-log](74-friction-log.md) — the skill-pack's Phase-0 dissection and the running log of what *couldn't* be made generic (and where each gap feeds).
- [208_meta-lessons-from-the-history-of-job-dispatch-issues](208_meta-lessons-from-the-history-of-job-dispatch-issues.md) — a retrospective that reads the whole build journal back: 11 grounded meta-lessons on how agent/job dispatch goes wrong, all reducing to one root cause (*the byte-author of the witness must not be the judged agent*).
- [209_out-of-loop-live-payoff-the-gap-numbers-and-the-experiment-registry](209_out-of-loop-live-payoff-the-gap-numbers-and-the-experiment-registry.md) — why routing the verdict to a non-agent consumer is the only positive half-plane, the **re-projection trap** (a static replay mints a *rate*, never a *payoff*), the **triple constraint** a valid live value experiment must meet, the **gap numbers** (the "45%" band — judge false-accept 0.35–0.55, G3 0.352, METR ~24pp, Wall-3 ~38%, E1 AUC 0.909) with worked examples + a visual, and the ranked live-payoff **experiment registry** (top pick `E-TAU2-WRITEADMIT`; the $0 `live_orchestrator_demo` to run first). Companion: [`benchmark/_experiments/out_of_loop_registry.md`](../benchmark/_experiments/out_of_loop_registry.md). Builds on [204](204_the-four-walls-where-verification-runs-out.md)/[206](206_proving-the-out-of-loop-verdict-the-frontier-lab-rigor-program.md).
- [210_the-supervise-config-seam](210_the-supervise-config-seam.md) — SHIPPED: the always-on supervisor's standing population policy becomes a `dos.toml [supervise]` table (`target` + `count_spinning_as_alive` + `reap_stalled`), read by BOTH `dos loop` and the watchdog driver, reported by `dos doctor`. The config-seam piece of [99](99_runtime-validation-and-the-actuation-boundary.md)'s supervisor Phase 3 (value-aware spawn ranking + acting-on-spin stay future). Same mechanism/policy split as the [207](207_dispatch-workflow-extraction-and-the-pickable-substrate-completion.md) `[cooldown]`/`[lifecycle]` seams.
- [392_hook-observation-contract-read-only-pure-meta-calls](392_hook-observation-contract-read-only-pure-meta-calls.md) — DESIGN slice for issue #231: publishes the hook observation predicate so known-empty read/meta calls avoid file-lane WARN noise while unknown or genuinely mutating calls stay must-observe. Separates file footprint, non-file effect, and PostToolUse stream coverage before matcher scoping.
- [394_the-forgeable-success-signal](394_the-forgeable-success-signal.md) — DESIGN note for issue #191: names the self-grading remediation counter, maps it to verify / commit-audit / effect evidence / dos-unstick, and keeps the generic detector in driver policy until domains converge on a stable evidence envelope.
- [395_residual-review-range-and-test-witness-plan](395_residual-review-range-and-test-witness-plan.md) — DESIGN plan for issue #204: selects a deterministic range claim fold and an orthogonal TWV residual axis, with acceptance witnesses, while deferring semantic hunk subtraction until it has an independent receipt.
- [396_shared-head-mutations-route-integration-to-a-worktree](396_shared-head-mutations-route-integration-to-a-worktree.md) — DESIGN decision for issue #222: classify HEAD/index/sequencer mutations as worktree-global, refuse them in shared main without exclusive ownership, and route integration into lifecycle-tracked DOS worktrees.
- [397_shared-checkout-concurrency-support-boundary](397_shared-checkout-concurrency-support-boundary.md) — DESIGN decision for issue #205: add short-lived per-path write intents at cooperative host seams, keep lane/index/HEAD controls distinct, and require dedicated worktrees for durable, dynamic-target, or unmanaged work.
- [216_executing-tau2-writeadmit-the-overclaim-slice-and-the-built-gate](216_executing-tau2-writeadmit-the-overclaim-slice-and-the-built-gate.md) — EXECUTES [209](209_out-of-loop-live-payoff-the-gap-numbers-and-the-experiment-registry.md)'s registry Item 1. The $0 over-claim **pre-check, run**: three independent detectors converge on a **13.6%** confident-over-claim slice (34/250 tau2; lexical ≡ write-verb to the index, the `effect_witness` kernel join a near-superset), **2.7× the 5% kill floor**, 12/12 adversarially verified (0% FP). Pinned as a tested SSOT (`overclaim.py --check`). The **$0 gate core BUILT** (`writeadmit/gate.py` reusing `witness_effect` + the converged claim detector; the floor makes a forgeable read-back unable to BLOCK), with the believe-vs-adjudicate frozen A/B fold (J=33 on the stand-in witness) + 9 tests; live driver gated behind `GEMINI_API_KEY`. The load-bearing finding: on the frozen corpus the witness is the **human label** (Wall-3 presence-not-correctness, [204](204_the-four-walls-where-verification-runs-out.md)) — only the **live env DB-hash** (`tau2-bench .../evaluator_env.py`) promotes it to a correctness witness, which is why the frozen number is a *rate* and only the live loop is a *payoff*. Verdict **GO_WITH_CHANGES** → **ran live, see [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)**. Lab-facing twin (`E-TAU2-RLVR-ADMIT`) shares the adapter, forks at the last function.
- [228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md) — **RUNS [216](216_executing-tau2-writeadmit-the-overclaim-slice-and-the-built-gate.md) live.** Wired the driver 216 left as a `NotImplementedError` (correcting its API note — `run_single_task` is the current tau2 entry, `run_task` the deprecated shim) and drove it on real tau2 tasks through Gemini. **Two runs, the contrast is the finding:** the **frozen over-claim slice re-run live → J = 0** (the frozen `-1` over-claims *evaporate* when a capable policy re-runs the task — it fails *honestly*; = the [199](199_the-curable-slice-conversion-experiment-the-data-never-collected.md) event-rate bound + [204](204_the-four-walls-where-verification-runs-out.md) §2 frontier-silence, confirmed live; the dual of "the pathology was the *producer's*, not the task's"); the **wide natural sample → J = 5** — the out-of-loop write-admission gate caught + **blocked 5 real over-claims** (agent: "successfully cancelled/updated/processed"; the env DB-hash said no) and **admitted all 9 honest writes**. Natural live over-claim base-rate **11.6%** ≈ the frozen 13.6%, now live + causal. **$0.89** total. Methodological lesson: don't replay a frozen over-claim slice through a capable policy; run live on a write-heavy natural distribution. **§5.1 HARDENED to saturation (2026-06-08):** flash swept the *whole* tau2 airline+retail universe + a second model (pro-2.5) → **J = 15 over 258 clean tasks, 2 models, $8.35** (`aggregate_live.py`, which RE-DERIVES per [232](232_hardening-the-out-of-loop-payoff-across-models.md) — never the stale cached bit). The over-claim rate by denominator (read all three — incidence, not "% better"): **5.8%** of tasks / **31%** of confident write-claims / **48%** of DB-*checkable* writes were claimed-success-but-wrong; gate caught all 15, **0 false blocks**, both model tiers. **§5.2 the honest limit (the part not to over-quote):** J is a *count of caught events*, NOT a "% better" — the *causal* downstream payoff was measured separately ([235](235_the-peer-b-handoff-executed-deltaB-measured.md)) and is **≈0 at the immediate next hop** (a capable peer self-recovers from the phantom). Detection is solid + generalizes; the *value* of acting on it is established only for non-re-verifying / multi-hop consumers (the next experiment). Synthesis: [234](234_the-non-distillable-reward-channel-lab-facing-proof.md).
- [229_the-peer-b-handoff-making-J-causal-plan](229_the-peer-b-handoff-making-J-causal-plan.md) — **the *commons* fork of [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)** (companion to [230](230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label.md)'s lab fork): turns docs/228's **counted** J=5 (confident-write × witness-refuted × blocked) into a *causal outcome delta*. docs/228 asserts the believe-vs-adjudicate split *arithmetically* — no second agent ever runs on what the gate published; this wires a real downstream **peer B** that inherits the gate's output and measures **ΔB = success(B \| adjudicate) − success(B \| believe)** on the over-claim slice (with the honest control: ΔB ≈ 0 on the 9 CONFIRMED rows, where the gate is a no-op by construction). **Feasibility verified** (tau2 `Task.initial_state` + telecom's programmatic `initialization_actions` make B's starting world controllable as data). **Design A** (narrated handoff — both arms run B against the same gold DB, only the inherited *message* differs; exactly how an LLM fleet hands off today) is the tight, low-confound contrast built first; **Design B** (replay A's tool-calls into B's env) is the richer DB-handoff follow-on. **Shipped so far:** the $0 pure constructor `writeadmit/peer_b.py` (`handoff`/`control_invariant_holds`/the live `InitialState` builder + `design_b_actions` sketch, 9 tests, honoring the [179](179_detector-self-labeling-harness.md) fold — the adjudicate-on-block handoff carries a fact A never authored). **Pending:** the paid live B run (~$0.40) + the `_report_causal` ΔB fold. Falsifiers stated up front (kill-1 ΔB≈0 = a true detection but no *measured* payoff at this hop; kill-2 a self-checking B recovers on its own — the [199](199_the-curable-slice-conversion-experiment-the-data-never-collected.md) event-rate bound one layer out).
- [230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label](230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label.md) — **the frontier-lab fork of [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)** ([216](216_executing-tau2-writeadmit-the-overclaim-slice-and-the-built-gate.md) §5's `E-TAU2-RLVR-ADMIT`, registry Item 2 / `E2L-1`). docs/228 framed J=5 as a *commons* result (a peer doesn't inherit the phantom write); this re-aims the **same witness + claim-extractor + `believe_under_floor` floor** at a *lab* consumer — a **reward-set admission filter** — forking `gate.admit` at the **last function only** (`writeadmit/rlvr_admit.py: admit_to_reward_set`). Re-folding *those same live rows*: of **15 confident "resolved" write-bids** a naive self-judged RLVR sampler banks as positives, **5 are poison** (the env DB-hash refutes — teaching the model to over-claim *more*), purged by the witness-gated filter → **acceptance precision 60%→100%, J=5 poison purged, ΔP=+40 pp**, $0. **Non-distillable** (the prized RLVR property): the accept/reject bit is a pure function of `db_match` (env world-state, agent authors zero bytes) — a forgeable answer can never flip it, pinned by `test_rlvr_admit.py` (10 tests). Keys on the **narrow write-correctness bit** (`db_match`), not the composite reward (airline/7: db_match=True yet reward=0.0 — §4a). **Payoff 2** (train two DPO/LoRA runs + measure the trained over-claim delta J₂) stays deferred — needs a GPU (the `E2L-3` wall); its loader seam (`accept`/`dispreferred` labels) is already in the code. Companion to [229](229_the-peer-b-handoff-making-J-causal-plan.md) (the *commons* causal fork): one witness, two consumers.
- 231_maintaining-not-improving-the-decay-prevention-charter — **relocated to the `dos-private` repo** ([231 there](https://github.com/anthony-chaudhary/dos-private/blob/master/231_maintaining-not-improving-the-decay-prevention-charter.md)) as strategy-genre prose (the decay-prevention *charter* — DOS's whole job is keeping trust from decaying as the horizon lengthens, a mission that compounds with model improvement). Carries no litmus / ships no mechanism, so it left the kernel repo. The kernel-membership test it introduced is folded into CLAUDE.md.
- [232_hardening-the-out-of-loop-payoff-across-models](232_hardening-the-out-of-loop-payoff-across-models.md) — **HARDENS [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)'s A-slice across models.** docs/228 measured the write-admission payoff on one model; this adds a second (gemini-2.5-**pro**) and the cross-run aggregator. **Headline: across 2 models the gate caught + blocked over-claims at an *identical* 8.3% rate (flash AND pro) — the phenomenon is not a flash artifact.** The load-bearing methodological catch (the **re-fold LAW**): a long resumable batch caches each row's `confident_write` *at the time it ran*, so when the claim-extractor changes mid-run the cache mixes pre/post-fix bits and a naive sum is stale (pro cached J=4 → re-folded **J=5**, the airline-8 over-claim the stale bit missed). `aggregate_live.py` + `index_models.py` RE-DERIVE every decision with the current extractor, never the cached bit. **Trust a re-fold over a cached bit when the code changed under it.** Companion to [235](235_the-peer-b-handoff-executed-deltaB-measured.md) (the causal fork): one witness, two questions (A-slice base-rate vs peer-B ΔB). Saturation total folded into [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md) §5.1: J=15 / 258 tasks / 2 models.
- [233_the-coordination-payoff-measured-live-arbiter-prevents-clobbers](233_the-coordination-payoff-measured-live-arbiter-prevents-clobbers.md) — **the SECOND value half-plane, and a measured POSITIVE** (where [235](235_the-peer-b-handoff-executed-deltaB-measured.md)'s over-claims ΔB came back ≈0, this one does not). [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md) measured referee-OVER-CLAIMS (a gate blocks a phantom write before a peer inherits it); this measures referee-BETWEEN-AGENTS by running **two** live Gemini agents into a genuine lost-update conflict on a shared tau2 reservation. **Headline: the arbiter prevents 6 real clobbers the naive flow lets through, witnessed by the env DB-hash** (~$1.5, 24 runs, 8 conflict pairs). Why this is positive where over-claims was ≈0: two agents racing on shared state is a conflict **neither can self-recover from** (unlike a single peer re-verifying an inherited claim — the [235](235_the-peer-b-handoff-executed-deltaB-measured.md) self-recovery). Pairs the coordination *rate* ([190](190_coordination-measured-and-the-f3-gateability-split.md): ≥5 concurrent same-file collisions @10s) with a *payoff*. Needs a **plurality** ([204](204_the-four-walls-where-verification-runs-out.md) §1 fleet-of-one).
- [234_the-non-distillable-reward-channel-lab-facing-proof](234_the-non-distillable-reward-channel-lab-facing-proof.md) — **the SYNTHESIS + the lab-facing proof** (re-files [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)→233 into the form a frontier RL/post-training team accepts on sight; no new mechanism). Leads with the one thing that survives small-n: a **proof**, not a sample — the reward-set admission label is a pure function of the env DB-hash the policy authors zero bytes of, so the policy's output text **provably cannot move it reject→accept** (the no-text-channel property, unit-pinned `test_rlvr_admit.py`). Then **honestly demotes every magnitude to an existence caveat**: J=5/ΔP=+40pp are same-corpus re-reads at anecdote-scale n, and **the one live causal payoff (peer-B ΔB) was run and came back ≈0** ([235](235_the-peer-b-handoff-executed-deltaB-measured.md)). ⚠ Carries a literature-citation discipline note (several specific external magnitudes are body-level, flagged `[confirm before external use]`). **This is the doc to read for the honest bottom line of the whole writeadmit program.**
- [235_the-peer-b-handoff-executed-deltaB-measured](235_the-peer-b-handoff-executed-deltaB-measured.md) — **EXECUTES [229](229_the-peer-b-handoff-making-J-causal-plan.md)** (the commons causal fork; renumbered from a 230-collision with the RLVR twin). Built `writeadmit/peer_b.py` + `peer_b_run.py` and ran the believe-vs-adjudicate A/B live with a real downstream **peer B** re-running A's *own* tau2 task (same-task replay — the dependent work IS the task). **The result: ΔB ≈ 0 at the immediate next hop** (over-claim slice: believe 3/5, adjudicate 3/5) — **because the peer self-recovers**: in 3/5 cases B was handed the phantom ("Q69X3R cancelled") and *re-verified + fixed the task anyway*. The control ΔB (−33%) is pure rollout noise (handoffs proven byte-identical; n=3 → ±33% floor — so the slice can't resolve a sub-33% effect). **Not a refutation** (detection is sound: J=5 reproduced on a fresh draw, claim-extractor recall-checked, no false blocks) — a **localization**: the [199](199_the-curable-slice-conversion-experiment-the-data-never-collected.md) event-rate bound one hop out. The cheap lever is **ruled out** — a `--trust-handoff` prompt telling B to trust prior state did NOT open ΔB (self-recovery stayed 3/5; ΔB went −20%, within noise): you can't *tell* a capable peer to be credulous. So the payoff lives where the consumer **structurally cannot** re-verify (a weaker/non-LLM reader, or a multi-hop chain). Second model (pro) replicates (also over-claims, also self-recovers) and **exposed a model-sensitive recall gap** ("you're all set", fixed `57b8b56`). Companion to [232](232_hardening-the-out-of-loop-payoff-across-models.md) (the A-slice base-rate across models): one witness, two questions.
- [236_recovery-is-a-confound-the-fleet-of-one-special-case](236_recovery-is-a-confound-the-fleet-of-one-special-case.md) — **why [235](235_the-peer-b-handoff-executed-deltaB-measured.md)'s ΔB came back ≈0, generalized into a methodological warning.** "The model will just recover on its own" is true *often enough to be dangerous*: it is a **base rate** quietly applied to a single **instance**, a **single-agent** property applied to a **fleet on shared state**, and a **lurking variable** that drives *every* intervention's measured payoff toward zero whenever the cure is tested at a consumer that can heal itself. This is the through-line that ties the over-claims ΔB≈0 ([235](235_the-peer-b-handoff-executed-deltaB-measured.md)) to the coordination *positive* ([233](233_the-coordination-payoff-measured-live-arbiter-prevents-clobbers.md)): the difference is exactly whether the consumer *can* self-recover (a single re-verifying peer can; two agents racing on shared state cannot). **Two cells now EXECUTED ($0):** the **non-LLM endpoint** (`peer_b.decide_nonllm` — a consumer with no re-verify channel) yields **ΔB=+1.0** on the same slice where the LLM gave ≈0 → the LLM's ≈0 was *entirely recovery laundering* (laundering gap +1.0); and the **H1 recovery survival curve** (`_recovery_survival.py`, 240 runs) shows that *even on feasible errors* "it'll recover" is false **~44%** of the time (the raw 0.717 tail is the [198](198_the-feasibility-witness-and-give-up-correctly.md)-class infeasibility confound — `create_filter` — split out; I nearly shipped it). The **weaker-LLM middle arm** was attempted (paid) and is **inconclusive** (flash-lite runtime-crashed + self-recovered like flash → "weaker by name" ≠ lower self-recovery `r`). Carries the **relative-impact 2×2** (reversibility × blast-radius → which DOS lever) and the analytic bridge `delta_b_of_r`. Rated in the capstone [243](https://github.com/anthony-chaudhary/dos-private/blob/master/243_rating-the-significance-of-the-measured-payoff-program.md). **The honest frame for "does DOS help?" — it helps where self-recovery is structurally impossible.**
- [250_payoff-2-the-trained-behavior-delta-rlvr-admit](250_payoff-2-the-trained-behavior-delta-rlvr-admit.md) — **closes the GPU half of the lab fork** ([230](230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label.md) §5's deferred Payoff 2; renumbered from a 231-collision with the decay charter). [230](230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label.md) measured *label quality* (the env-grounded reward set purges the poison over-claims, ΔP +40pp); this measures *trained-model behavior* — **train two models on the poisoned vs the witness-cleaned reward set and read whether the poison set yields a model that over-claims MORE (J₂)**, a flipped model behavior no frozen rate can show. **$0 proxy result: J₂ = +60%** (the deferred real-weights DPO/LoRA tune is the open arm; the loader seam — `accept`/`dispreferred` labels — is already in `rlvr_train.py`). Builds on [230](230_the-lab-facing-twin-rlvr-admit-the-non-distillable-reward-label.md) (Payoff 1) / [216](216_executing-tau2-writeadmit-the-overclaim-slice-and-the-built-gate.md) §5 (the lab-twin spec).
- [254_the-freshness-sort-key-prefer-new-work-over-churn](254_the-freshness-sort-key-prefer-new-work-over-churn.md) — **the picker-substrate ORDERING leaf** (`dos.pick_priority` + `dos pick-priority`; the [168](168_picker-substrate-and-quiet-completion-the-missing-syscalls.md)/[207](207_dispatch-workflow-extraction-and-the-pickable-substrate-completion.md) cluster's missing *which-fresh-unit-first*). Closes the **churn** the job repo measured live — 24h, 19 dispatch runs, **only 1 shipped a pick (5.3%)**, 18 re-confirming known drains while fresh plans sat un-picked — by fixing an *ordering* gap the gates couldn't: the host plan-sort key was `(priority, status, id)`, with **no freshness term**, so a never-attempted plan and one drained 18× sorted identically and ties broke on alphabetical id. `cooldown` only GATES after a window; this ORDERS what's left. `classify(unit, AttemptSummary) -> PickPriority` (NEVER_ATTEMPTED / ATTEMPTED) folds the SAME OP_ATTEMPT history into a `sort_key` the host appends AFTER its own key — never-attempted first, then least-recently-tried (LRU). **The safety invariant: a within-tier TIE-BREAKER** — it can only reorder inside a priority/status tier, never gate a unit in/out, never reorder across tiers (a P1 attempted unit still beats a P2 fresh one), fail-open to fresh. The same refuse-MORE-only floor as the overlap policy, on the *ordering* side. Parameter-free (no config table); attempt-count + quiet-incomplete demotion left as flagged follow-ons.
- 242_signal-to-noise-the-kernel-as-a-distrust-filter — **relocated to the `dos-private` repo** ([242 there](https://github.com/anthony-chaudhary/dos-private/blob/master/242_signal-to-noise-the-kernel-as-a-distrust-filter.md)) as a strategy-genre synthesis note (reads the whole kernel through one lens — *signal-to-noise*: a fleet's noise is every self-narrating byte, the signal is the small set of facts an agent could not author; DOS raises the ratio by refusing to read narration). No new mechanism, so it left the kernel repo.
- 243_rating-the-significance-of-the-measured-payoff-program — **relocated to the `dos-private` repo** ([243-rating there](https://github.com/anthony-chaudhary/dos-private/blob/master/243_rating-the-significance-of-the-measured-payoff-program.md)) as a strategy-genre capstone assessment of the [228](228_running-tau2-writeadmit-live-the-out-of-loop-payoff-measured.md)→250 payoff arc (rates research 8/10 vs product 6/10; headline: *the value of a distrust verdict is bounded by the consumer's own error rate*). No new mechanism, so it left the kernel repo. **NB: the staying [243_the-fleet-trajectory-benchmark](243_the-fleet-trajectory-benchmark-from-our-own-corpus.md) is a *different* doc that shares the number — bare "docs/243" elsewhere usually means that engineering doc.**
- [217_the-cross-vendor-hook-dialect-seam](217_the-cross-vendor-hook-dialect-seam.md) — DESIGN. The 2026-06-07 audit ("how well does DOS work with Gemini/Codex/Cursor?") finding + the plan to close it. DOS has **three** binding surfaces: the kernel syscalls (vendor-*irrelevant* — they adjudicate git, not the agent), the **MCP server** (works on all three TODAY — all are MCP clients — but advisory: a tool the agent *calls*, not a gate), and the **hooks/PEP** (`dos hook pretool`/`posttool`/`stop`), which emit **exclusively the Claude Code `hookSpecificOutput` envelope**. So detection+adjudication are cross-vendor; *enforcement* is CC-only. The **reversal** this overturns: as of early–mid 2026 all three rivals now ship a deny-capable pre-tool hook (Gemini `BeforeTool`, Codex `PreToolUse`, Cursor `beforeShellExecution`) — the "CC is the only PEP" belief is stale. The fix is a **`HookDialect` renderer seam** (the third instance of the `judges`/`overlap_policy` pure-protocol+by-name-resolver pattern, on the *output* side): `decide()` returns a dialect-neutral `HookVerdict`; a per-host renderer (`claude-code` default + `gemini`/`codex`/`cursor`) emits the right envelope, selected by `--dialect`. Cheap because the vendors copied CC's payload shape — the deltas are event-names + envelope, not logic. Phase 1 gate = the CC dialect reproduces today's bytes exactly (existing hook suite green, unchanged). Companion zero-code win shipped alongside: per-host MCP wiring in [`src/dos_mcp/README.md`](../src/dos_mcp/README.md). Generalizes [165](165_dos-into-claude-code-the-runtime-binding-roadmap.md) off Claude Code onto three more hosts; preserves the [191](191_proactive-dos-banking-the-verdict-before-the-failure.md) byte-author floor.
- [releases/](releases/) — per-version release notes.

---

> **The pattern across all of it:** *mechanism is the kernel; policy is data a
> workspace declares; workflow is the host's.* If a doc seems to contradict that,
> it's the doc that's stale — [CLAUDE.md](../CLAUDE.md) is the contract.
