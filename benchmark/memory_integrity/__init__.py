"""benchmark/memory_integrity — the bad-memory integrity benchmark (docs/316).

A consumer of the kernel, never part of it (the one-way arrow): it imports the
memory-recall driver and scores its write/recall gates against a corpus whose
labels are fixed by a constructed git history, not by narration.
"""
